dist: trusty
language: ruby


sudo: required
services:
  - docker


script: |
  .travis.d/build.sh &&
  cd .travis.d && bundle install --path vendor/bundle &&
  bundle exec ruby pr_autodeploy.rb

env:
  global:
    - HACKGPROJECT_REV="18afeecdafdf3cbb40f6246bbb4d57d71508d74b"

    - DOCKER_ID_USER="hackgt"

